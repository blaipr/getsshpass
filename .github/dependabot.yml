version: 2
updates:
  # The only dependencies are the GitHub Actions used in CI workflows;
  # there is no package manager (getsshpass is a dependency-free bash script).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
